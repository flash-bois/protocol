import { Connection, PublicKey } from '@solana/web3.js'
import { assert } from 'chai'
import { describe } from 'mocha'
import { swap } from '../../pkg/protocol'

describe('global', async () => {
  it('swap', async () => {
    swa()
  })
})
